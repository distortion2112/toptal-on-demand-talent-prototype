import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './styles.css'

// Demo prototype — all data is hard-coded mock. No backend.
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
